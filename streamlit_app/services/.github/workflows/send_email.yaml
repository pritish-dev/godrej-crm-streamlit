# ⚠️ DEAD FILE — This workflow is in the wrong location.
# GitHub Actions only reads workflow files from /.github/workflows/ at the
# repository ROOT. This file (nested inside streamlit_app/services/) is
# NEVER executed by GitHub.
#
# The active workflow is at: .github/workflows/godrej-email.yaml
#
# This file should be deleted from the repository.
# (Kept here as a placeholder to document the dead-file issue until
#  the repo owner deletes it via git rm.)
